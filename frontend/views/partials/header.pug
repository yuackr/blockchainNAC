header.header
    .header__wrapper
        .header__column
            a(href=routes.home)
                h2 NAC Service
            h3 영남대 네트워크
        .header__column
            ul
                if !loggedUser
                    li
                        a(href=routes.join) Join
                    li
                        a(href=routes.login) Log In
                else
                        a(href=routes.logout) Log Out