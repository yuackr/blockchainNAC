// Global
const HOME = '/';
const JOIN = '/join';
const LOGIN = '/login';
const LOGOUT = '/logout';
const ERROR = '/error';
const NO_CHANNEL = '/no_channel';

const routes = {
  home: HOME,
  join: JOIN,
  login: LOGIN,
  logout: LOGOUT,
  error: ERROR,
  no_channel: NO_CHANNEL,
};

export default routes;
